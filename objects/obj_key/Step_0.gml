// obj_item Step Event
if (picked_up && owner != noone) {
    x = owner.x + offset_x;  // Follow player's x position with offset
    y = owner.y + offset_y;  // Follow player's y position with offset
}